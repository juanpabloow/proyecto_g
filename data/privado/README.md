# Aquí van los archivos con datos reales o sensibles.

Todo el contenido de esta carpeta está en `.gitignore` y **no se sube al
repositorio** (regla [R-7](../../docs/reglas-de-negocio.md)).

Los datos de ejemplo, ficticios, van en `data/contratos_ejemplo.csv`.
